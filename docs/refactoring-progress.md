# Mini-IM 重构执行记录

返回 [项目入口](../README.md)。

目标：按 [2026-09-07 基线评估](architecture-review-2026-09-07.md) 依次修复正确性、
整理客户端职责与持久恢复、完善工程验证。本地 Git 基线为 `ce5a78b`。
本文件是当前任务、验证结果和剩余工作的唯一记录；日常操作命令维护在
[开发指南](development.md)，长期约束维护在 [AGENTS.md](../AGENTS.md)。

## 当前阶段

源码核对日期：2026-09-08；正确性修复及截至自动重连批次的已验证改动已提交为 `edb61e8`。
文件任务持久恢复已单独提交为 `6258944`；每个已验收模块与完成阶段分别提交。
原生连接职责拆分已提交为 `befc5b1`。
同步协调及无进展页恢复已提交为 `62c37d5`；文件任务协调与独立重试已提交为 `3e08735`。
服务端控制写入持久去重与事务失败回滚已提交为 `09b0971`。
原生控制写入队列与重启恢复已提交为 `a7adf3e`。
页面控制操作与真实桌面验证已提交为 `43400db`。
最新实现批次为 [文件续传与存储故障恢复](#文件续传与存储故障恢复--2026-09-08)：
上传恢复核对实际文件与已提交进度，存储失败保留原意图；等待完成确认的上传重连后重新核对服务端进度。
64 个服务端测试、48 个完整原生网络场景及 1 个补充场景、5 个原生组件与 7 个构建目标通过，本批独立提交。
服务端取消协议、多设备、真实服务端进程宕机、完整桌面文件恢复和整体工程保障继续未完成。
历史结果按批次保留；当前缺口见 [下一步与未验证项](#下一步与未验证项)，文档整理记录位于文末。

阶段须满足整体验收条件才可标记完成。
“已验收”只适用于相应批次和覆盖范围；后续修改相关路径后，须重新验证才能更新结论。

| 阶段 | 交付与验收条件 | 当前状态 |
| --- | --- | --- |
| 正确性 | 六个已复现缺陷修复并纳入正式回归；原有测试继续通过；下载按实际落盘与校验结果确认完成 | 基线六项已修复并纳入回归；已有业务及页面回归证据，重连接入后的全部原生网络场景通过 |
| 客户端职责 | 分离连接、同步和文件任务；Qt 管理恢复参数；消息缓存与连续游标共同保存；消息及文件意图跨重启恢复；按发送进度分批读文件；双客户端验证断线、换用户、重启、重复确认与续传 | 进行中：文件调度、同步存储、消息及文件任务持久保存、重连计时已拆出并验证；连接收发已拆出并验证；同步协调已拆出并验证；文件任务协调已拆出并验证；服务端及原生控制写入已提交，页面控制操作已通过真实桌面检查；文件存储故障及续传回退已验收，48 个原生场景及 1 个补充场景通过；服务端取消、多设备及服务端进程重启仍待完成 |
| 工程保障 | 统一环境与依赖脚本；类型检查、构建、协议拆包及跨端测试进入自动验证；空目录构建；测量聊天与文件并行传输时的延迟及资源占用 | 待推进：已有原生组件及跨端联调入口；类型检查纳入自动验证、统一脚本、空目录构建及并发测量未完成 |

## 当前实现与验收边界

| 范围 | 实现与已验证内容 | 尚需完成 |
| --- | --- | --- |
| 原生状态与任务 | [对象映射](../client/core/model/eventmapper.cpp)、[状态存储](../client/core/sync/statestore.cpp) 和 [消息队列](../client/core/message/outbox.cpp) 已拆出；[同步协议](../proto/sync.proto) 逐条提供 global_seq，[同步协调](../client/core/sync/coordinator.cpp) 管理在线事件、历史补拉及重试。[文件协调](../client/core/file/coordinator.cpp) 管理文件任务及独立重试。对象、事件标识和连续位置共同保存；待确认文本消息保存原意图并重试；[控制队列](../client/core/session/writecoordinator.cpp) 已接入其他 8 类业务操作的保存与顺序重试，42 个原生网络场景通过。组件及真实进程检查覆盖回滚、乱序、重启、重复确认和用户隔离；未完成上传的磁盘进度回退、存储异常与原意图恢复已验证 | 文件取消状态协议、同意图多设备并发及已发布文件损坏恢复；缓存增长后的启动成本 |
| 会话与页面恢复 | [原生连接](../client/core/quic/connection.cpp) 管理连接与数据流；Qt 从按地址、用户和设备分开的缓存读取恢复参数；页面连接、消息、文件及控制操作使用完成回调，初始状态恢复待确认与失败操作；原生恢复通过，页面控制操作已在真实 Qt WebEngine 下验证保存失败、重试、已读衔接和换用户 | 多设备及真实服务端进程重启；桌面文件操作与完整恢复流程 |
| 页面与运行环境 | 聊天页面已用索引取代 .at(-1)，类型检查与打包通过；Qt Sql 和 SQLite 插件已用于组件和原生联调；Windows 旧客户端崩溃导致监听停止收包的问题已修复 | 空目录构建和完整部署；统一环境脚本与自动验证；其他平台实测 |

当前客户端跨重启恢复覆盖已保存的文本消息和文件任务；8 类控制写入的服务端请求结果和客户端本地队列均已接入，
原生组件及真实网络回归通过；页面完成回调和操作状态展示已接入。
[页面接口封装](../web/src/api/bridge.ts) 覆盖连接、消息、文件、建会话、成员变更、已读和撤回。
本批真实桌面检查覆盖控制操作；文件页面的实际传输与完整恢复仍需独立验收。

## 正确性修复批次

以下记录对应正确性修复的首批改动；后续批次会复验受影响的路径。

| 范围 | 该批次改动 | 正式验证入口 |
| --- | --- | --- |
| 用户隔离与终态保护 | 切换用户清空所属展示状态；同用户重连保留展示；重复或晚到的消息不能恢复已撤回、已焚毁正文 | [页面状态测试](../web/tests/session.test.ts) |
| 同步标识与内容清理 | 同用户在线与重放复用事件标识；按用户清理焚毁消息副本；全员焚毁后清理正文；重复清理及旧结构迁移保持正确结果 | [服务端可靠性测试](../server/tests/test_reliability.py) |
| 文件完成与下载确认 | 文件完成及聊天消息共同提交；重试修复旧的缺失消息；下载由接收端提交实际大小和摘要；Qt 临时落盘、校验后替换目标；拒绝超长上传和完成后追加内容 | [服务端可靠性测试](../server/tests/test_reliability.py) · [原生下载测试](../client/tests/downloadsink_test.cpp) |

服务端事务已支持嵌套调用共同回滚，`FileFinish` 与 `FileUpdated` 增加下载确认及元数据字段，
Python 协议文件已重新生成，Qt 已重建。
原生下载组件覆盖临时文件续写；客户端完整任务跨进程恢复仍属于下一阶段。

## 正确性修复验证 · 2026-09-07

下述历史验证使用的本机环境：Windows、Python 3.14.3、Node.js 24.13.1、npm 11.8.0、
CMake 3.27.2、Visual Studio 2022、Qt 6.11.0 `msvc2022_64`。
Python 使用 `tmp/architecture-review-deps` 中的 aioquic 1.3.0、protobuf 6.33.6；
Qt 使用已有 `build/client_qt611` 目录。环境版本说明验证条件，不代表已验证所有安装组合。

| 已执行命令（项目根目录） | 结果 | 覆盖及限制 |
| --- | --- | --- |
| 设置上述临时依赖后，`python -m unittest discover server/tests -v` | 33 个测试通过 | 原有 25 个及新增 8 个；业务、存储、旧结构升级和下载确认；下载发送使用替身，无真实接收客户端 |
| `npm --prefix ./web test` | 6 个测试通过 | 用户切换、同用户重连、撤回/焚毁事件在消息前后到达及重复重放 |
| 在 `web/` 中执行 `./node_modules/.bin/vue-tsc.cmd --noEmit` | 失败，1 处 TS2550 | 当时 `chat-view.vue` 使用 `.at(-1)`，类型配置为 ES2020；后续修复与通过结果见原生同步存储批次 |
| `npm --prefix ./web run build` | 通过，Vite 7.3.2 | 页面打包；未包含类型检查或界面交互 |
| `cmake --build ./build/client_qt611 --config Release --target mini_im_client mini_im_download_tests` | 两个目标均构建通过 | 本机已有目录增量构建，未验证空目录配置与运行部署 |
| `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure`（上传拆分前） | 1 个测试目标通过 | 原生下载：完整性、截断、错误摘要、超长数据、临时文件续写、部分写入、写入/目标错误、重复完成；未经过 MsQuic 网络 |

协议生成补充记录：本轮修改 `file.proto` 后使用已有 vcpkg 的 protoc 33.4 生成
`server/protocol/pb/file_pb2.py`，再调用生成工具的 `patch_imports` 修正包内导入。
这验证了本机生成产物可导入及测试通过；新环境的统一生成流程仍需随依赖脚本一起验证。

## 原生文件模块与真实联调 · 2026-09-07

已将原生代码编译为共用的 `mini_im_core` 静态库。
桌面程序和 `mini_im_native_driver` 复用这份库及同一 Bridge；
联调驱动通过本机测试命令接口控制操作，实际业务数据经 MsQuic 与 aioquic 的 QUIC 连接传输。
测试使用临时数据库、文件、证书与随机端口，无需启动默认端口上的开发服务。

首轮真实传输复现了连续下载挂起：截断下载留下的接收状态以原生流句柄作为索引，
句柄释放后复用会让后续下载进入旧状态。现已改用连接内唯一的 QUIC 流编号，
并加入并发下载和失败后再次下载的回归场景。

[上传模块](../client/core/file/uploadstream.cpp) 每流最多保留 2 个待完成发送，每块最多 65536 字节，
总待发送数据上限为 131072 字节；完成回调释放缓冲后才继续读取。
会话配置关闭 MsQuic 内部发送缓冲，让发送缓冲的生命周期随传输确认推进，
语义依据见 [MsQuic 官方说明](https://microsoft.github.io/msquic/msquicdocs/docs/Streams.html#send-buffering)；
文件业务完成仍由服务端验证并返回应用确认。

| 本轮已执行检查 | 结果及范围 |
| --- | --- |
| 构建 `mini_im_client`、`mini_im_native_driver`、`mini_im_upload_tests`、`mini_im_download_tests` | 四个 Release 目标通过；复用本机已有构建目录 |
| `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` | 2 个目标通过；下载完整性，以及上传缓冲上限、恢复偏移、源文件变化与发送失败 |
| 临时依赖环境下执行 `python ./tools/test_native_flow.py` | 5 个场景通过：消息/重复发送/已读/撤回/离线补拉，下载元数据延后 300 毫秒，3 个并发下载，截断和错摘要后再正常下载，上传连接中断 |
| 服务端业务回归与页面状态回归 | 33 个服务端测试、6 个页面测试通过 |

上传缓冲测试使用受控传输完成回调，真实联调使用两个独立 Qt 进程；
Vue 渲染和页面点击没有包含在该联调中。
下载用例分别使用 1048593、98304、32128 字节数据，上传中断用例为 2097152 字节，
在下一批数据将使累计接收达到或超过 65536 字节时注入断连；这些数据用于正确性检查，不构成吞吐或聊天延迟结论。
本轮原始结果保存在本机 `tmp/native-integration/20260907-044654/`，不纳入 Git；
维护的复现入口是 [test_native_flow.py](../tools/test_native_flow.py)。

## 下载调度与文件意图 · 2026-09-07

[服务端下载调度](../server/quic/download.py) 每条流的未确认发送数据最多 131072 字节，
每个连接最多同时调度 8 条下载，因此这些下载流的待确认数据总量最多 1048576 字节。
每次文件读取最多 65536 字节，放在线程中执行；已确认数据释放缓冲后再继续读取。
连接关闭、对端取消和文件错误会停止后续读取；达到任务数上限时返回可重试错误。

Qt 每次异步接收最多处理 65536 字节，处理或写入后通知 MsQuic 继续。
下载元数据未到达时保留一个未完成接收，控制流继续处理消息；
文件流头部限制为 512 字节。上述数字限制的是应用批次和下载发送缓冲，
不代表整个进程、传输库或操作系统的内存上限。

调度模块通过一个集中适配器读取 aioquic 发送缓冲状态。
这部分依赖已固定的 aioquic 1.3.0 内部字段；升级依赖时须运行
[调度兼容性测试](../server/tests/test_download_scheduler.py) 和真实联调，不能仅依赖导入成功。

[文件仓储](../server/storage/repo/file_repo.py) 已校验同一用户文件意图对应的会话、名称、大小、
摘要、方向和下载来源；重试不能替换这些内容。
完成状态保持不变，没有业务变化的初始化、进度和完成重试不再追加同步事件。
下载来源保存到 `file_transfers.source_file_id`，并支持旧数据库添加字段。
旧下载记录缺少来源时保留记录和文件，以冲突错误拒绝猜测来源；用户可明确发起新的下载。

| 本轮验证 | 实际结果 |
| --- | --- |
| `python -m unittest discover server/tests -v`（临时依赖环境） | 43 个测试通过；新增无确认暂停、丢包与乱序确认、8 条并发上限、恢复偏移、文件/网络错误、意图冲突、旧结构升级及无变化重试 |
| `python ./tools/test_native_flow.py`（临时依赖环境） | 7 个真实 Qt/QUIC 场景通过；在原 5 项上增加下载等待元数据时仍收聊天消息，以及等待中断连再重新下载 |
| Release 客户端和联调驱动构建；`ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` | 构建通过；2 个原生组件目标通过 |

真实联调新增的暂停下载场景输入为 2097152 字节，等待元数据时断连场景为 262144 字节。
检查同时确认原目标文件受到保护、释放等待后内容完全一致；
本轮记录目录为 `tmp/native-integration/20260907-091202/`，原始输出不纳入 Git。
未将这些场景计为程序重启恢复、页面交互或并发性能验收。

## 原生同步存储与进程重启 · 2026-09-07

本轮继续使用前述 Windows、Python 3.14.3、Qt 6.11.0 和已有构建目录。
[状态存储组件测试](../client/tests/statestore_test.cpp) 验证业务对象、事件标识和连续位置在同一事务中回滚，
重开数据库后重试仍正确；晚到的消息保持撤回/焚毁状态，已读位置不回退，
地址、用户和设备分别保存状态，会话和文件旧版本不能覆盖新状态。

真实联调增加三个场景：强制结束客户端后恢复消息、已读与撤回状态并接收离线消息；
保留一个历史缺口、先收到后续在线消息，再强制结束进程，重启后补齐缺口且不重复推送；
同一 Qt 进程从 Bob 切到 Carol 再切回 Bob，检查消息、会话与恢复位置隔离。
这些场景覆盖已接收状态恢复，待确认写入和文件任务的跨重启恢复仍需实现。

重启测试另复现了服务端接收停滞：向已退出客户端推送后，
Windows 返回 ConnectionResetError 10054，监听仍开放但没有待处理的接收操作，新客户端连接超时。
诊断保存在本机 tmp/native-integration/20260907-100502/；
[监听入口](../server/quic/endpoint.py) 现在在 Windows 下创建并配置 UDP 套接字，
关闭端口不可达报告，再交给 aioquic；正式启动和联调共用该入口。
SIO_UDP_CONNRESET = 控制 UDP 套接字是否向接收调用报告对端端口不可达的 Windows 配置，
语义见 [微软接口说明](https://learn.microsoft.com/en-us/windows/win32/winsock/winsock-ioctls#sio_udp_connreset-opcode-setting-i-t3)。
当前 Python 的 socket.ioctl 未暴露该控制码，使用具有明确参数类型的 Winsock 调用。

| 已执行检查 | 结果与范围 |
| --- | --- |
| 构建 mini_im_client、mini_im_native_driver、mini_im_state_tests；运行 CTest | 构建通过；3 个原生组件目标通过，包括状态存储回滚、乱序、恢复及原文件测试 |
| 临时依赖环境执行 python -m unittest discover server/tests -v | 43 个测试通过；事件身份测试增加逐条全局位置与历史重放一致的断言 |
| npm --prefix ./web test；在 web 中执行 vue-tsc --noEmit；npm --prefix ./web run build | 8 个状态测试、类型检查及打包通过；新增初始状态恢复后已读不重复扣减、文件旧版本不覆盖完成状态 |
| 临时依赖环境执行 python ./tools/test_native_flow.py | 10 个真实 Qt/QUIC 场景通过，包含原 7 项与新增 3 项；输出在 tmp/native-integration/20260907-100744/，不纳入 Git |

本轮 windeployqt 因无法覆盖已有 icuuc.dll 返回失败；
单独复制新增 Qt6Sql.dll 与 sqldrivers/qsqlite.dll 后，组件及真实网络测试通过。
这只证明本机已有部署目录可运行新增依赖；完整部署仍须在退出占用该目录的程序后重新验证。
本轮未验证 Vue 页面点击、多设备网络、服务端进程重启、自动重连、未确认写入/文件任务跨重启恢复，
也未执行空目录构建、并发性能测量或其他平台验证。

## 消息发送意图与确认恢复 · 2026-09-07

本批次沿用前述 Windows、Qt 6.11.0、Python 3.14.3 和已有构建目录。
[消息队列](../client/core/message/outbox.cpp) 在联网发送前保存请求标识、发送意图、正文及失败状态；
进程重启并以相同身份连接后，同步追平再继续原意图。发送成功确认后清空队列正文，
撤回/焚毁处理也在状态事务内清理发送副本，失败事务不提前移除待确认内容。
具体重试间隔、错误处理及操作方式统一见 [开发指南](development.md#消息发送与恢复)。

| 验证方式 | 结果与覆盖范围 |
| --- | --- |
| 构建桌面、原生驱动和状态测试；运行 CTest | 构建通过；3 个原生组件目标通过，状态测试增加原意图冲突、重新打开、失败显式重试、重复确认、正文清理及事务回滚 |
| 临时依赖环境执行 `python ./tools/test_native_flow.py` | 15 个场景通过；新增提交前失败恢复、已提交但确认丢失后的重启恢复、超时与重复确认下的顺序、失败重启后显式重试、切换用户隔离待确认消息；校验重试标识/正文相同且服务端只生成 1 条消息 |
| `npm --prefix ./web test`；在 web 中执行 `vue-tsc --noEmit`；`npm --prefix ./web run build` | 9 个状态测试、类型检查与页面打包通过；增加待确认/失败消息按用户恢复，原生状态确认后清除展示 |
| Playwright CLI 驱动 Edge，访问本机 Vite 页面并注入受控 Qt 接口 | 检查回调前保留草稿和禁止重复提交，保存失败保留草稿，保存成功后清空，失败原因显示及沿用原意图重试；截图检查布局。该检查不经过实际 QWebChannel 或 QUIC |

原生联调输出位于 `tmp/native-integration/20260907-102948/`；
浏览器检查材料位于 `tmp/ui-qa/`，截图为 `output/playwright/message-failed.png`，均被 Git 忽略。
文档回填时重跑页面测试、类型检查和打包，三项退出码均为 0；结果位于 `tmp/ui-qa/web-results.json`。
上述本机材料用于追溯当次检查，不是新环境的安装或运行前提。

本批次未修改服务端生产代码；43 项服务端测试的最近完整运行仍属上一批次。
自动重连、会话失效后的自动重新认证、文件任务与其他写入跨重启恢复、
完整 Qt WebEngine 页面路径、多设备和服务端进程重启仍未验收；空目录部署与性能测量仍待完成。

## 自动重连与会话恢复 · 2026-09-07

本轮先用真实客户端复现：消息已经入库但确认丢失，连接被服务端关闭后，
客户端停留在断开状态，8 秒内没有重新登录。复现输出为
`tmp/native-integration/20260907-142357/`。

[重连组件](../client/core/quic/recovery.cpp) 管理递增等待与登录超时；
会话层在连接关闭后读取本地恢复位置，重新登录并同步，再重发原待确认消息。
401 会清除旧会话标识；主动断开停止重连；控制流中断和应用响应超时也进入恢复。
具体间隔统一维护在 [开发指南](development.md#消息发送与恢复)。

连接代次 = 每次关闭连接后递增的编号；排入 Qt 线程的原生回调携带其所属编号，
过期回调不能影响新连接或新用户状态。关闭流程先处理流，再释放连接；
资源释放后不得再次调用对应连接或流，依据见
[MsQuic ConnectionClose](https://microsoft.github.io/msquic/msquicdocs/docs/api/ConnectionClose.html)
和 [回调执行说明](https://microsoft.github.io/msquic/msquicdocs/docs/Execution.html)。

| 检查 | 环境、命令与结果 |
| --- | --- |
| 桌面及原生组件 | 沿用 Windows、Qt 6.11.0、Visual Studio 2022 和已有构建目录；按开发指南构建桌面、驱动、下载、上传及状态目标成功；`ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` 的 3 个目标通过 |
| 真实网络 | Python 3.14.3 配合临时依赖执行 `python ./tools/test_native_flow.py`，21 个场景通过，用时 58.981 秒；在原 15 项上增加断线重发、会话失效、取消重连后切换用户、登录无响应、控制流复位、心跳无响应 |
| 页面及改动检查 | 9 个页面状态测试、`vue-tsc --noEmit` 与 Web 打包通过；`git diff --check` 通过。浏览器交互仍采用上一批次结果，本轮没有重跑完整页面交互 |

网络结果位于 `tmp/native-integration/20260907-143047/`，不纳入 Git。
登录无响应场景丢弃一次 Welcome 并等待 10 秒超时；应用无响应场景把测试服务器的
心跳间隔设为 1 秒，保持传输确认但不返回业务响应，验证检测后重新连接并收取消息。
这些测试没有重启服务端进程，也没有验证多设备网络、长时间重连退避或资源稳定性。
本轮还修复了生成文件的换行检查失败：通过生成工具规范 `file_pb2.py` 为 LF，
并核对规范前后的 Python 语法结构一致；服务端业务实现未改动。

提交前复核：相同 Python 临时依赖环境下，服务端 43 个测试再次通过，输出为 `tmp/precommit-server-tests.log`。
本次提交包含基线之后积累的正确性、文件调度、状态存储、消息恢复、自动重连及文档整理；
后续已验收模块分别提交，客户端职责和工程保障阶段继续保持未完成。

原生连接的收发及同步调度仍在会话类；文件任务和其他写入的持久恢复、
空目录部署及并发测量继续保留为后续任务。
文档同步后复核 7 份 Markdown 的 UTF-8、130 处内部文件/章节链接和 5 个构建目标，均通过；
检查输出为 `tmp/documentation-audit/recovery-result.json`，整体改动空白检查通过。

## 文件任务持久恢复 · 2026-09-07

[文件任务存储](../client/core/file/taskstore.cpp) 保存原初始化与完成请求、意图、路径和校验信息，
账号切换时随状态库隔离；初始化与完成状态提交后才向页面报告本地接受。
上传在重新初始化后按服务端位置续传，下载按已有临时文件长度续传；
已发布下载在重试完成确认前重新校验目标，摘要失败的显式重试从 0 重新下载。
任务完成和本机取消保持终态，晚到的结果不能把它们改回待处理。

本轮沿用 Windows、Qt 6.11.0、Python 3.14.3、既有 Release 构建及临时 Python 依赖。

| 检查 | 结果与范围 |
| --- | --- |
| 桌面及原生组件 | 构建桌面、联调驱动、状态测试通过；按开发指南运行 CTest，3 个目标通过；新增文件任务写入失败、身份与元数据冲突、账号隔离、重开及完成/取消不回退 |
| 完整原生网络运行 | `python ./tools/test_native_flow.py` 运行 27 个场景，全部通过，用时 87.420 秒；新增上传进程重启续传、下载部分落盘后重启、发布后完成确认丢失、初始化确认丢失、源文件变化后原意图重试、本机取消后重启 |
| 文件边界补充 | 同一入口用 `--test` 选择损坏下载原意图重试、超过 8 个活跃任务的等待队列、切换用户隔离上传，3 个新增场景通过；生产代码未再改变，当前维护入口共有 30 个场景 |
| 页面 | 10 个状态测试、`vue-tsc --noEmit` 及页面打包通过；浏览器真实点击检查上传/下载在回调前保留输入并禁止重复提交、失败保留输入、成功清空，以及原任务重试和取消；注入受控 Qt 接口，未经过实际 QWebChannel |

上传重启输入为 2056192 字节，注入断连阈值 262144 字节，断开时服务端已有非零进度；
下载重启输入为 1028096 字节，保存 65536 字节临时文件后强制结束进程，
断言新请求恢复偏移等于已落盘长度，服务端只有 1 条对应意图记录。
9 个排队下载各使用 90112 字节，在暂停期间验证只有 8 个初始化请求进入服务端，
同时完成聊天往返，释放暂停后 9 个文件均一致；以上属于正确性检查，不是吞吐测量。

完整运行输出为 `tmp/native-integration/20260907-181744/`，
补充场景为 `tmp/native-integration/20260907-182124/`；
浏览器脚本位于 `tmp/ui-qa/`，布局截图为 `output/playwright/file-tasks-failed.png`，均未入库。
客户端取消只保证本机停止和不自动恢复，保留临时数据与服务端记录；
服务端取消状态、机器断电或磁盘数据与记录不一致的协调、真实服务端进程重启仍需完善。
完整 Qt WebEngine 页面路径、多设备、任务调度进一步拆分和性能测量仍未完成。
提交前复核 7 份 Markdown、134 处内部文件/章节链接和 5 个构建目标通过，整体改动空白检查通过；
检查输出为 `tmp/documentation-audit/file-recovery-result.json`。

## 原生连接职责拆分 · 2026-09-07

[原生连接](../client/core/quic/connection.cpp) 统一管理 MsQuic 的配置、连接、控制流、
上传流对象和下载接收批次；[会话类](../client/core/session/sessionmanager.cpp)
通过控制数据、文件流事件和发送方法协调业务，不再直接调用 MsQuic 或保存原生流句柄。
连接代次检查继续隔离排入 Qt 线程的旧回调；接收元数据尚未到达时保持下载暂停，
会话类完成处理后才释放该批次，每批最多 65536 字节。

关闭和析构时保留原生回调上下文及流对象，直到同步关闭连接停止工作线程回调；
随后释放余下流句柄。工作线程收到的对端流立即登记，避免退出时遗漏尚未送达 Qt 线程的流。
上传启动失败后也保留发送对象到连接关闭，防止已启动流仍在回调时删除其持有者。
资源生命周期依据 [ConnectionClose](https://github.com/microsoft/msquic/blob/main/docs/api/ConnectionClose.md)、
[StreamClose](https://github.com/microsoft/msquic/blob/main/docs/api/StreamClose.md)
及 [回调执行规则](https://github.com/microsoft/msquic/blob/main/docs/Execution.md) 核对。

本轮环境为 Windows、Qt 6.11.0、Visual Studio 2022、CMake 3.27.2、Python 3.14.3，
使用既有 `build/client_qt611` 与 `tmp/architecture-review-deps` 依赖目录。

| 检查 | 结果与范围 |
| --- | --- |
| 最终构建 | `cmake --build ./build/client_qt611 --config Release --target mini_im_client mini_im_native_driver mini_im_upload_tests` 通过；完整输出为 `tmp/connection-extraction-build-final.log` |
| 原生组件 | `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` 的 3 个目标通过，用时 0.46 秒；输出为 `tmp/connection-extraction-ctest.log` |
| 定向网络检查 | 提取后先运行 4 个场景通过，覆盖挂起下载断开、控制流复位、上传进程重启和 9 个下载排队；随后新增 2 个正常退出场景通过 |
| 完整网络检查 | 设置上述临时依赖后执行 `python ./tools/test_native_flow.py`，最终构建的全部 32 个场景通过，用时 103.690 秒；输出为 `tmp/connection-extraction-full.log`，分场景证据为 `tmp/native-integration/20260907-192028/` |

新增场景分别在下载等待元数据和登录等待 Welcome 时正常退出 Qt 进程；
检查进程退出码为 0，再启动后验证原下载任务及消息收取。
退出等待限定 5 秒，挂起或崩溃会使测试失败；该检查补充此前强制结束进程的恢复场景。
定向证据分别位于 `tmp/native-integration/20260907-191709/` 和
`tmp/native-integration/20260907-191847/`；全部临时输出均未入库。

本轮没有修改页面、服务端或协议定义；没有重新运行页面或服务端业务测试。
同步调度、文件任务协调、多设备和真实服务端进程重启仍未完成；
正常退出场景不证明长时间重连后的资源占用，也不替代完整 Qt WebEngine 页面验收。

提交前检查：7 份 Markdown 的 UTF-8、138 处内部文件/章节链接、11 段 PowerShell 示例、
5 个构建目标和整体改动空白检查通过；文档证据为 `tmp/documentation-audit/connection-extraction-result.json`。

## 同步协调与无进展页恢复 · 2026-09-07

[同步协调组件](../client/core/sync/coordinator.cpp) 从会话类接管在线/历史事件应用、分页、请求重试和同步就绪判定。
消息及文件任务通过同一就绪判定继续执行；同步计时器独立于消息发送重试。
会话类提供当前会话的请求头与原生发送接口，连续位置直接读取状态存储，不再维护另一份位置副本。
事件仍在状态事务提交后交付业务；历史去重不阻止文件控制元数据抵达等待中的下载。

拆分前先增加真实网络复现：服务端丢弃一个在线事件，随后一次历史响应返回空页，
旧客户端清掉活动请求并等待 8 秒仍未补回缺失消息，消息与文件意图停留在本地。
失败证据为 `tmp/sync-coordinator-before.log` 和 `tmp/native-integration/20260907-192821/`。
修复后遇到空页或无进展页仍有缺口时保留原请求，按开发指南的间隔重试；
收到匹配的末页并补齐连续位置后才释放待处理写入。旧页响应不能结束另一活动请求，
服务端宣告的位置不能直接推进本地连续位置。

本轮沿用 Windows、Qt 6.11.0、Visual Studio 2022、CMake 3.27.2、Python 3.14.3、
既有 `build/client_qt611` 构建目录与 `tmp/architecture-review-deps` Python 依赖。

| 检查 | 结果与范围 |
| --- | --- |
| 最终构建 | 按开发指南构建桌面、联调驱动及 4 个原生测试目标，共 6 个目标通过；输出为 `tmp/sync-coordinator-build-final.log` |
| 原生组件 | `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure`，4 个目标通过，用时 0.64 秒；新增 [同步协调测试](../client/tests/synccoordinator_test.cpp) 检查分页与晚到响应、空页及无进展页、写入回滚、停止和文件元数据重复送达 |
| 定向网络检查 | `python ./tools/test_native_flow.py` 配合 `--test` 选择两个新增场景通过，用时 8.490 秒；空页恢复后同一消息/文件意图继续发送；205 条离线消息以 200 条和 5 条分两页补拉，按原顺序各交付 1 次 |
| 完整网络检查 | 设置上述临时依赖后执行 `python ./tools/test_native_flow.py`，全部 34 个场景通过，用时 129.030 秒；输出为 `tmp/sync-coordinator-full.log`，分场景证据为 `tmp/native-integration/20260907-193756/` |

定向日志为 `tmp/sync-coordinator-targeted.log`，分场景结果位于
`tmp/native-integration/20260907-193546/`。联调入口保存同步响应的请求标识和事件位置，
空页场景检查下一次响应沿用原请求；等待期间服务端未收到被阻塞的消息或文件初始化。
这些日志及组件输出 `tmp/sync-coordinator-ctest.log` 均未入库。

本批没有修改服务端、协议定义或页面，未重新运行相应业务/页面测试。
文件任务协调、其他写入的持久恢复、多设备及真实服务端进程重启、完整页面和工程保障仍待推进；
205 条历史消息检查验证分页正确性，不作为缓存启动或并发性能测量。

提交前检查：7 份 Markdown 的 UTF-8、144 处内部文件/章节链接、11 段 PowerShell 示例、
6 个构建目标和整体改动空白检查通过；文档证据为 `tmp/documentation-audit/sync-coordinator-result.json`。

## 文件任务协调与独立重试 · 2026-09-08

[文件协调组件](../client/core/file/coordinator.cpp) 接管任务保存、初始化/完成确认、
上传与下载状态、续传、失败重试及本机取消；[会话类](../client/core/session/sessionmanager.cpp)
只转发文件操作与确认，提供当前会话的请求头和发送接口。
同步组件通过就绪通知控制文件调度，原生连接直接向文件组件交付流事件；
文件重试使用独立计时器。开发日志函数移入 [共享日志](../client/core/logging.cpp)，保持原日志位置和开关。

本轮先在旧客户端复现：下载完成确认丢失后，向独立测试缓存加入触发器，
仅拒绝 `message_outbox.attempts` 的更新；发送消息后旧共用计时器停止，
下载任务停在 `finishing`，等待 8 秒仍未完成。
复现日志为 `tmp/file-coordinator-before.log`，分场景证据位于
`tmp/native-integration/20260908-121420/`。
修复后文件按原完成请求重试，消息意图仍保存在本地且未被误报发送成功；
本场景不模拟整库不可用或磁盘断电。

环境为 Windows、Qt 6.11.0、Visual Studio 2022、CMake 3.27.2、Python 3.14.3，
沿用既有 `build/client_qt611` 与 `tmp/architecture-review-deps` 依赖目录。

| 检查 | 结果与范围 |
| --- | --- |
| 原生组件 | `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure`，最终构建的 4 个目标通过，用时 0.78 秒；输出为 `tmp/file-coordinator-ctest-final.log` |
| 定向网络 | `python ./tools/test_native_flow.py` 配合 `--test` 选择 5 个场景通过，用时 26.033 秒；覆盖消息尝试写入失败后的文件确认重试、空同步页等待、下载续传、取消后重启和账号切换 |
| 最终构建 | 按开发指南构建桌面、联调驱动及 4 个原生测试目标，共 6 个目标通过；输出为 `tmp/file-coordinator-build-final.log` |
| 完整网络 | 设置上述临时依赖后执行 `python ./tools/test_native_flow.py`，全部 35 个场景通过，用时 117.512 秒；输出为 `tmp/file-coordinator-full.log`，分场景证据为 `tmp/native-integration/20260908-122647/` |

新增下载输入为 71680 字节；测试确认目标内容一致、本机文件任务最终为 `completed`，
同时消息任务仍为 `pending` 且尝试次数为 0。
定向日志为 `tmp/file-coordinator-targeted.log`，分场景结果为
`tmp/native-integration/20260908-122126/`，以上临时输出均未入库。

本轮未改页面接口、服务端或协议定义，未重跑其业务/页面测试。
本机取消继续保留服务端记录和临时文件；服务端取消协议与故障数据协调仍待完善。
其他写入的持久恢复、多设备及真实服务端进程重启、完整页面和工程保障继续保持未完成。

提交前检查：7 份 Markdown 的 UTF-8、151 处内部文件/章节链接、11 段 PowerShell 示例、
6 个构建目标和整体改动空白检查通过；文档证据为 `tmp/documentation-audit/file-coordinator-result.json`。

## 控制写入持久去重与事务失败回滚 · 2026-09-08

本轮为其他客户端写入恢复补齐服务端前提。[控制写入服务](../server/services/control/service.py)
统一接入建会话、增加成员、移除成员、加入、退出、改名、已读和撤回 8 类操作；
[请求结果存储](../server/storage/repo/control_write_repo.py) 按用户与请求标识保存操作类型、内容摘要和原确认。
认证会话与设备变化不改变去重键；同一请求改变操作或内容返回 409，确定失败保留原确认。
业务变化、同步事件与结果共同提交后才回复；重放不重新执行或新增同步事件，原事件继续由同步入口补拉。
重复建会话沿用原 `client_conv_id` 时也不再新增事件；现有消息和文件意图去重沿用各自业务路径。

先加入真实 QUIC 回归，修复前 2 个测试均失败：首次改名已提交但确认丢失，后续改名完成后重开
服务端监听与数据库，旧请求重放会把新名称改回旧名称；同一请求改变正文也被再次执行。
复现输出为 `tmp/control-write-before.log`，2 个测试用时 0.707 秒。
随后用另一个数据库连接持有读取事务，使最终提交确定地返回数据库忙；旧实现留下未结束事务，
复现输出为 `tmp/control-write-commit-before.log`，用时 0.027 秒。
[事务封装](../server/storage/sqlite/db.py) 现把最终提交也纳入异常回滚，最外层失败完整回滚，
嵌套失败只回滚所属范围；SQLite 已自动回滚时保留原异常。

本批环境：Windows、Python 3.14.3、aioquic 1.3.0、protobuf 6.33.6；Python 使用
`tmp/architecture-review-deps`，Qt 使用既有 Release 联调驱动，客户端生产代码和协议未变。

| 本轮执行 | 结果及覆盖 |
| --- | --- |
| `python -m unittest discover -s server/tests -v` | 54 个测试通过，用时 2.101 秒；原有 43 个及新增 11 个；输出 `tmp/control-write-server-final.log` |
| [控制写入业务测试](../server/tests/test_control_write_service.py) | 8 个测试覆盖全部操作、交错重放、重新打开、确定失败、用户隔离、操作/内容绑定、旧库重复升级、可重试失败回滚及最终提交失败释放事务 |
| [控制写入网络测试](../server/tests/test_control_write_network.py) | 3 个真实 QUIC 场景；丢弃原确认后重开服务端与数据库、换设备标识重连仍保留后续改名；变更内容返回 409；结果写入故障返回 503，业务及事件回滚，原请求重试成功且确认到达时其他数据库连接可见全部结果 |
| `python ./tools/test_native_flow.py` | 最终事务修复后 35 个原生网络场景全部通过，用时 118.568 秒；证据目录 `tmp/native-integration/20260908-124718/`，输出 `tmp/control-write-native-final.log` |

首次原生回归在最终事务修复前通过 35 个场景，用时 118.004 秒，输出 `tmp/control-write-native.log`，
证据目录 `tmp/native-integration/20260908-124443/`；最终修复后另行复验，不沿用该结果。
数据库忙回归使用 DELETE 日志模式和 0 毫秒等待以稳定触发提交失败，生产配置仍为 WAL/NORMAL；
该场景不代表机器断电或整盘写满测试。网络重开在同一 Python 测试进程内完成，尚未验证真实服务端进程重启，
换设备标识重连也不代表多设备并发场景。请求结果表升级保留业务数据，但无法补建升级前旧请求的原确认；
结果清理策略未实现，当前随数据库保留。

客户端控制写入本地队列、持久重试、页面完成回调仍待接入；完整 Qt WebEngine、文件服务端取消及数据协调、
工程自动验证、全业务写队列和性能测量继续未完成。本轮未修改客户端或页面，未重新构建或运行页面测试。
原始日志位于忽略的 `tmp/`，不入库且不是复现前提；正式入口为已入库测试。
提交前 7 份 Markdown 的 UTF-8、161 处内部链接、11 段 PowerShell 示例、6 个构建目标及空白检查通过；
文档证据为 `tmp/documentation-audit/control-write-result.json`。

## 原生控制写入队列与重启恢复 · 2026-09-08

[控制写入存储](../client/core/session/writestore.cpp) 在按地址、用户、设备隔离的缓存中新建 `control_outbox`，
保存请求、操作、业务内容、首次时间、尝试次数及确认结果；旧库新增该表不清空原数据。
[控制写入协调](../client/core/session/writecoordinator.cpp) 管理独立的 500 毫秒检查计时器，
一次发送 1 个控制请求，距上次发送至少 5000 毫秒才重试；在初次同步或补偿缺口时等待追平。
登录后只更新请求路由，原请求标识和业务内容保持；断连停止内存调度，本地记录保留。
建会话、增加/移除成员、加入/退出、改名、已读和撤回全部接入，同一建会话意图在本地核对内容并复用原请求。

原生调用成功表示本地已保存，即使后续记录尝试或发送失败也不改成拒绝保存。
确定失败和成功确认均为终态，晚到确认不再覆盖；可重试错误仍为待确认，新的用户操作采用新请求。
新增 `initialStateLoaded.controlWrites` 与 `controlWritesChanged`，向 Bridge 提供业务状态、次数和原因，
不暴露协议字节；Vue 尚未接入这些状态及相关操作的异步回调。

先以旧客户端复现：Bob 的已读请求被原生接口接受，服务端返回 503 后客户端进程终止；
重启后 8 秒仍未补发已读，回归失败。输出 `tmp/control-queue-before.log`，用时 8.508 秒，
目录 `tmp/native-integration/20260908-130103/`。修复后的定向运行中，6 个场景通过，
保存失败场景被正确拒绝，但测试错误文案匹配和 SQLite 连接清理导致测试失败；修正测试后由最终完整运行复验。

本批环境沿用 Windows、Python 3.14.3、aioquic 1.3.0、protobuf 6.33.6、Qt 6.11.0 msvc2022_64、
CMake 3.27.2 与已有 Release 构建目录；Python 使用 `tmp/architecture-review-deps`。

| 本轮执行 | 结果及覆盖 |
| --- | --- |
| 按开发指南构建桌面、驱动及 5 个组件目标 | 共 7 个目标通过；`tmp/control-queue-build-final.log`；新增旧库检查后单独重建控制测试通过，`tmp/control-queue-upgrade-build.log` |
| `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` | 5 个目标全部通过，用时 0.83 秒；`tmp/control-queue-ctest-final.log`；[控制队列测试](../client/tests/controlwrite_test.cpp) 覆盖顺序、原请求及首次时间、确定结果不回退、账号隔离、插入/记录尝试/保存确认失败、旧缓存重复升级保留消息与会话 |
| `python ./tools/test_native_flow.py` | 42 个真实原生网络场景全部通过，用时 123.709 秒；目录 `tmp/native-integration/20260908-131034/`，输出 `tmp/control-queue-native-final.log` |

新增 7 个真实客户端场景覆盖：已读提交前失败后重启；建群提交后丢确认重启且事件不重复；
退出丢确认后按原请求重试，再执行排队加入；成员增删及改名按原顺序跨重启恢复；
撤回等待同步并在切换账号时隔离；本地插入失败拒绝且不发请求；确定拒绝跨重启保留，新的用户操作使用新请求。
原生组件的确认写入失败回归使用真实 SQLite 触发器，不等同于整盘写满或机器断电。
控制队列只保证自身请求顺序，消息与文件仍由各自协调组件调度；当前结果不宣称跨队列统一顺序或多设备并发验收。

本轮未改服务端生产代码、协议和 Vue 源码；服务端完整 54 项验证仍属前一批，页面测试未重跑。
下一步补齐页面操作完成回调和控制写入状态展示，再继续完整 Qt WebEngine、多设备及真实服务端进程重启，
以及文件服务端取消、磁盘故障协调、统一工程脚本和并发测量；客户端及工程保障阶段继续保持进行中。
本机原始证据位于忽略的 `tmp/`，不提交且不是复现前提；测试源码和文档命令为正式入口。
提交前检查：7 份 Markdown 的 UTF-8、171 处内部链接、11 段 PowerShell 示例、7 个构建目标和空白检查通过；
文档证据为 `tmp/documentation-audit/control-queue-result.json`。

## 页面控制操作与真实桌面验证 · 2026-09-08

建群、私聊、加群、成员增加/移除、退群、改名、已读和撤回的 9 个页面接口改为等待 Qt 完成回调。
建会话表单在保存成功后关闭，保存失败保留输入，回调期间禁止重复提交与关闭；
邀请成员同样在本地保存后清空输入。页面显示控制请求的待确认、自动重试和确定失败原因，
并通过账号初始化状态恢复；旧账号的回调不得清除新账号输入。
已读避免在保存期间重复生成意图；原请求确认后继续提交后来读到的消息。
本地保存失败可手动“标为已读”重试；确定失败保留状态并等待用户重新操作。
页面卸载时取消事件订阅和未刷新的消息批次；Qt 初始化失败时不生成演示成功结果。

环境：Windows、Qt 6.11.0 `msvc2022_64`、Python 3.14.3、Node.js 24.13.1；
Python 使用忽略目录 `tmp/architecture-review-deps` 中的 aioquic 1.3.0、protobuf 6.33.6。
Qt 使用已有构建，加载本批最终 `web/dist`，通过已安装的 Playwright CLI 连接本机页面调试端口。

| 已执行检查 | 结果与边界 |
| --- | --- |
| 新增接口测试在修复前运行 | 10 项失败、原有 10 项通过，复现同步返回及缺失方法伪造成功；输出 `tmp/control-ui-before.log` |
| `npm --prefix ./web test` | 22 项通过；9 个控制接口的延迟成功/失败回调、方法缺失、原生初始化失败，以及操作状态按用户恢复；输出 `tmp/control-ui-tests.log` |
| 在 `web/` 执行 `vue-tsc --noEmit`，随后 Web 打包 | 均通过，输出 `tmp/control-ui-types.log`、`tmp/control-ui-build.log`；构建不替代交互验收 |
| 按开发指南运行桌面隔离环境与页面检查 | 24 个步骤及数据库断言通过，运行于 17:29:13–17:29:47；真实 Qt 页面、QWebChannel、原生连接和服务端 QUIC 路径 |
| 截图检查 | 1200 × 800 桌面截图中表单错误、操作状态和聊天布局可读，未发现重叠；图片位于 `output/playwright/20260908-171914/` |

正式入口为 [隔离桌面环境](../tools/desktop_fixture.py)、[页面检查](../tools/test_desktop_ui.py)
与 [浏览器步骤](../tools/desktop_ui.js)，命令集中在 [开发指南](development.md#真实桌面页面联调)。
桌面检查覆盖建群、私聊及加群保存失败后重试，回调期间重复提交只产生 1 次原生调用，
邀请和移除成员、改名 503 后沿用相同请求和内容重试、确定失败显示、发送后撤回、退群再加入。
已读保存失败不推进服务端位置；手动重试后推进到 2，丢失位置 3 的应用确认时复用原请求，
随后推进到 4，各位置只产生 1 个请求意图。切换为 Bob 隐藏 Alice 的失败操作，切回恢复。
结果及原始日志保存在 `tmp/desktop-integration/20260908-171914/`，`ui-result.json` 为成功；
临时数据库、截图和日志不提交，也不是新环境复现前提。

补充复验：为正式入口加入 Qt 启动等待，并要求丢失确认的位置 3 至少出现 2 次原请求尝试；
在全新隔离环境再次执行，24 个步骤及数据库断言通过，时间为 17:36:18–17:36:52。
本轮最终脚本证据位于 `tmp/desktop-integration/20260908-173551/ui-result.json`；
Playwright CLI 版本为 `1.63.0-alpha-2026-08-31`。两个隔离环境均正常停止，临时数据已清理。
提交前 7 份 Markdown 的 UTF-8、179 处内部链接、13 段 PowerShell 示例、7 个构建目标和空白检查通过；
文档证据为 `tmp/documentation-audit/control-ui-result.json`。


该环境使用单个真实 Qt 桌面按顺序切换用户；对端消息由真实业务服务生成后经 QUIC 推送。
未覆盖同时在线的多设备、独立服务端进程宕机、桌面文件页面全流程及空目录部署。
本轮未改原生或服务端生产代码，未重跑上一批的 42 个原生网络、5 个组件及 54 个服务端测试，
不把旧结果标为本批验证；客户端职责和工程保障整体阶段继续保持未完成。

## 文件续传与存储故障恢复 · 2026-09-08

本批已完成下述范围的验收，按模块独立提交。
[文件服务](../server/services/file/service.py) 初始化未完成上传时核对实际文件长度，
取磁盘长度、数据库已提交字节数与声明大小的最小值；文件缺失回到 0，未提交尾部被截断。
[文件仓储](../server/storage/repo/file_repo.py) 保存回退位置并增加版本，与对应同步事件共同提交；
过期恢复与修正属于同一外层事务，直接响应携带最后一个版本的事件标识。
上传数据循环处理短写，完整写入并刷新文件及操作系统缓冲后才提交进度。
写盘/进度提交失败关闭上传连接，未提交尾部在原意图重连时处理；文件初始化和完成的存储异常返回 503。
未完成文件大小或摘要不符时标记 `failed_integrity`、位置归 0，显式重试沿用原文件意图。
已完成文件保持原完成状态，不允许迟到数据或续传截断其内容。

[Qt 文件协调](../client/core/file/coordinator.cpp) 在重新连接时把等待完成确认的上传恢复到待初始化状态，
重新核对服务端现存内容后继续；初始化请求、完成请求和文件意图均保持不变。
下载的目标文件校验和完成确认恢复语义保持原有要求。

环境：Windows、Python 3.14.3、Qt 6.11.0 `msvc2022_64`、CMake 3.27.2、Visual Studio 2022；
Python 依赖为忽略目录 `tmp/architecture-review-deps` 中的 aioquic 1.3.0、protobuf 6.33.6。
客户端使用已有构建目录重新构建，服务端与每个原生场景使用隔离数据。

| 已执行检查 | 结果与范围 |
| --- | --- |
| [新增服务端存储回归](../server/tests/test_file_storage_recovery.py) 在修复前运行 | 7 项均失败，0.126 秒；复现短写 3 字节却记录 16 字节、缺失/截短续传偏移、未提交尾部、零写入及摘要失败；输出 `tmp/file-storage-before.log` |
| `test_upload_finish_restart_rechecks_server_progress` 在 Qt 修复前运行 | 真实客户端重启后直接发完成请求，未修复截短文件，8.947 秒后失败；输出 `tmp/file-storage-finish-before.log` |
| `python -m unittest discover server/tests -v` | 64 项通过，3.143 秒；新增 7 项存储恢复和 3 项真实 QUIC 文件控制错误回归，后者覆盖初始化回滚、磁盘不可用、完成与文件消息共同回滚；输出 `tmp/file-storage-server.log` |
| `cmake --build` 本机 7 个既有目标 | 均通过，含桌面、原生驱动与 5 个组件目标；输出 `tmp/file-storage-build.log` |
| `ctest --test-dir ./build/client_qt611 -C Release --output-on-failure` | 5 个组件通过，0.93 秒；输出 `tmp/file-storage-ctest.log` |
| 新增原生网络场景首轮 | 5 项通过，53.826 秒；文件截短、缺失、未提交尾部、文件刷新和进度提交失败；输出 `tmp/file-storage-native-targeted.log` |
| `python ./tools/test_native_flow.py --client ./build/client_qt611/Release/mini_im_native_driver.exe` | 48 项通过，200.825 秒；含既有 42 项和新增 6 项恢复场景，已覆盖上传完成等待期间重启；输出 `tmp/file-storage-native-final.log` |
| 补充原生摘要损坏回归 | 1 项通过，4.406 秒；保留损坏的已收前缀后续传，校验失败，再显式重试原意图完成；输出 `tmp/file-storage-native-digest.log` |

完整命令维护在 [开发指南](development.md#验证)，原生单场景选择使用 `--test`。
新增原生输入包含 2056192 字节上传，以及 128512 字节的完成等待/摘要损坏恢复；
断点修复后核对实际完整内容、原文件/请求标识和恰好 1 条文件聊天消息。
数据库重开、过期接管、同步事件一致性、修正事务失败回滚和已完成文件不回退由服务端回归覆盖。
全部原生回归证据为 `tmp/native-integration/20260908-180717/`，摘要损坏补充场景为
`tmp/native-integration/20260908-180842/`，两轮共覆盖当前 49 个原生场景。
原始证据位于忽略的 `tmp/`，不提交且不作为复现前提；测试源码为正式入口。

本批不改变协议定义与 Vue 源码，未重新执行页面测试或完整桌面交互。
机器断电、独立服务端进程宕机、同一上传意图在多设备同时传输、服务端取消协议和已发布文件损坏处理仍未验收。
同步文件刷新仍在事件循环中执行；本轮恢复文件大小及用时不作为并发吞吐或聊天延迟结论，性能工作继续保留。
客户端职责与工程保障整体阶段保持进行中。
提交前检查：7 份 Markdown 的 UTF-8、185 处内部链接、13 段 PowerShell 示例、7 个构建目标和空白检查通过；
文档证据为 `tmp/documentation-audit/file-storage-result.json`。

## 下一步与未验证项

按当前依赖顺序推进，完成后同时更新阶段摘要与相应验证批次：

| 顺序 | 工作范围 | 验收重点 |
| --- | --- | --- |
| 完成客户端恢复 | 继续完整桌面恢复；完善服务端取消状态、同一上传意图的多设备并发及已发布文件损坏恢复 | 多设备、重复确认、客户端和服务端重启、完整续传及任务取消 |
| 完善页面接口 | 继续验证实际 Qt 文件页面与完整恢复交互；控制操作的完成回调及错误反馈已验收 | 从真实页面验证连接、消息、会话、已读、撤回和文件操作及错误反馈 |
| 完成工程保障 | 统一环境及依赖脚本，把类型检查、构建、协议拆包和跨端测试纳入自动验证；落实完整业务事务的统一写入入口 | 空目录构建，数据升级，聊天与文件并行传输的延迟及资源测量 |

服务端写队列虽已启动，业务仓储仍直接操作连接。
客户端哈希计算及发布目标文件仍同步执行，服务端上传写盘和业务事务仍在事件循环中执行；
后续结合真实并发测量处理延迟。当前缓存读取全部已保存消息，消息数量增加后的启动成本仍需测量。
文件调度测试不覆盖以上事项，客户端与工程保障阶段继续保持未完成。

## 文档整理记录

2026-09-07 已将入口、操作命令、长期规则、设计说明和当前进度分开维护；
`PLANS.md` 只保留计划导航，历史评估保留当时结论并标注适用时点。
修正历史评估中的本机绝对链接，说明临时证据未入库；
README 与 REPORT 不再重复维护功能完成表和历史测试数字。

首轮整理记录：7 份 Markdown 可按 UTF-8 读取，79 处内部链接可解析，
10 段 PowerShell 示例通过语法解析，文档范围的 Git 空白检查通过。

本次复核补充 Qt Sql 依赖、缓存目录和配置、浏览器演示模式与真实联调的区别；
当时将同步存储和页面改动列入待验收表，保留各历史批次当时的测试结果。
统一阅读入口、返回链接、计划跳转与文档更新规则；日常命令仍只维护在开发指南。
本次检查环境为 Windows、Python 3.14.3、PowerShell 7.6.5：7 份 Markdown、109 处内部链接（含章节跳转）及 11 段 PowerShell 示例检查通过，构建目标均存在，文档空白检查通过。
核对使用 Python 读取文件、解析链接并对照 CMake 目标，使用 PowerShell Parser 检查示例，执行 git diff --check -- "*.md"；整理期间 91 个已跟踪非文档文件内容保持不变。
本机检查输出位于被忽略的 tmp/documentation-audit/，不作为复现前提；示例语法检查未执行依赖安装、首次配置或服务启停。

同步存储验证记录回填后复核：7 份 Markdown、111 处内部链接、11 段 PowerShell 示例通过，构建目标匹配，整体改动空白检查通过。

本次消息队列回填与文档复核 · 2026-09-07：修正待确认消息和页面回调的过时摘要，
补齐开发指南的任务导航、消息恢复操作及其边界，并同步设计说明和长期接口规则。
使用 Python 3.14.3 执行 `python ./tmp/documentation-audit/check_docs.py`，
通过 PowerShell 7.6.5 的 `Parser.ParseInput` 检查命令示例，
并执行 `git diff --check -- "*.md"`：7 份 Markdown 按 UTF-8 读取正常，
126 处内部文件/章节链接、11 段 PowerShell 示例及 5 个构建目标检查通过，
整理前后核对的 112 份源码与配置文件内容一致。
检查脚本和输出留在被忽略的 `tmp/documentation-audit/`；首次安装、服务启停和完整部署未在本轮文档检查中执行。

旧文档可通过本地基线提交 `ce5a78b` 追溯。
后续更新直接修改本文件的阶段和待办状态；新增验证记录须写明日期、命令、环境、结果及未覆盖范围。
